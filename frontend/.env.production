# In production the frontend is served by the NestJS backend on the same origin.
# Leave VITE_API_BASE empty so API calls go to the same host/port (relative URLs).
VITE_API_BASE=

# Subpath under which the app is served by the reverse proxy.
# Must match the nginx location block and NestJS serveRoot.
VITE_BASE=/hivecards-manager/
